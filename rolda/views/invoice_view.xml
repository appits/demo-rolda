<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_invoice_form_inherit_rolda" model="ir.ui.view">
        <field name="name">account.move.form.inherit.rolda</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="ref" position="after">
                <field name="amount_total_currency" invisible="True"/>
                <field name="amount_by_group_currency" invisible="True"/>
                <field name="amount_untaxed_currency" invisible="True"/>
            </field>
        </field>
    </record>
</odoo>